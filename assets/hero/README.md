Optional homepage loop: save a silent MP4 here as **`hero-loop.mp4`**.

The home hero already looks for this file. If it is missing, the page uses `assets/screenshots/mariner-radar.jpg` plus a CSS radar sweep.
